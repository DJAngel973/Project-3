<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <!-- Definimos appenders (dónde se registrarán los logs) -->
    <Appenders>
        <!-- Appender para la consola -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %c - %msg%n" />
        </Console>

        <!-- Appender para registrar logs en un archivo -->
        <File name="FileLogger" fileName="logs/application.log" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %c - %msg%n" />
        </File>
    </Appenders>

    <!-- Configurar loggers -->
    <Loggers>
        <!-- Logger para la aplicación, nivel INFO por defecto -->
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="FileLogger" />
        </Root>

        <!-- Logger específico para CourseService con nivel DEBUG -->
        <Logger name="co.edu.devSenior.service.CourseService" level="debug" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="FileLogger" />
        </Logger>
    </Loggers>
</Configuration>